/* Задача

Этот метод, который должен возвращать результат деления первого аргумента на второй, 
не всегда возвращает правильные значения. Исправьте это.

*/

const solve = (x, y) => {
  console.log(x / y);
  return x / y;
};

solve(2, 4);
